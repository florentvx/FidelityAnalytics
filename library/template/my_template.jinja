<!DOCTYPE html>
<html lang="en">
<head>
    <title>Fidelity Analytics</title>
    {# meta data displayed in header's bar of internet navigator #}
</head>
<body>
    <h1>Fidelity Analytics</h1>
    User: {{ user_name }}<br>
    Date: {{ date }}<br>
    
    <br>
    --------------- <br>
    <br>

    {# total stats #}
    
    Total Value {{ total_data.total_value }} <br>
    Total Value Without Cash {{ total_data.total_without_cash }} <br> 
    dividends sum {{ total_data.dividends_sum }} <br>
    dividends ratio {{ total_data.dividends_ratio }} <br>
    dividends expected {{ total_data.dividends_expectation }} <br>

    <br>
    --------------- <br>
    <br>

    {# stats #}
    {% for item in data -%}
    
        Name {{ data[item].name }} <br>
        Market Price {{ data[item].market_price }} <br>
        Quantity {{ data[item].quantity }} <br>
        Amount {{ data[item].amount }} <br>
        Purchased price {{ data[item].purchased_price }} <br>
        return {{ data[item].return }} <br>
        pnl {{ data[item].pnl }} <br>
        dividends sum {{ data[item].dividends_sum }} <br>
        dividends ratio {{ data[item].dividends_ratio }} <br>
        dividends expected {{ data[item].dividends_expectation }} <br>
        
        <br>
        ---------------<br>
        <br>

    {% endfor %}
    

</body>
</html>